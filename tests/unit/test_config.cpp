// tests/unit/test_config.cpp — T2.2: missing file is not fatal, unknown key warns and continues,
// malformed number keeps the default, lists/booleans/blank values, CLI overrides config.
// Phase 0 registers no tests on purpose: T0.1 Step 3 expects `OK  0 tests`.
#include "check.hpp"
