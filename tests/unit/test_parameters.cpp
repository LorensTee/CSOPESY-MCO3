// tests/unit/test_parameters.cpp — T2.1: clamping with explicit ClampReports, setText's trim + internal-run
// rules, and the three TextResult outcomes (Ok / Empty / NonAscii) with the state unchanged on refusal (D15).
// Phase 0 registers no tests on purpose: T0.1 Step 3 expects `OK  0 tests`.
#include "check.hpp"
