// tests/unit/test_scroll.cpp — T4.2: scrollOffset wraps at artWidth + bandWidth; sliceRow is always
// exactly bandWidth columns.
// Phase 0 registers no tests on purpose: T0.1 Step 3 expects `OK  0 tests`.
#include "check.hpp"
