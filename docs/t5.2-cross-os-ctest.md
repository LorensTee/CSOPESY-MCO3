# T5.2 — cross-OS verification record

**Task.** Plan §5 T5.2: *"cross-OS matrix — Linux + Windows verified; macOS as a second opinion"*, with the
evidence being **`ctest` on each** and **the CI run URLs**. This file is that record. It is the global T5.2
artifact; the macOS-specific interactive pass stays in `docs/W4_KIM_PROGRESS.md` (W4's file), the Windows
A1–A11 acceptance pass stays in `docs/t5.1-windows-acceptance.md`, and this file ties the three together.

**Commit under test.** `dbfc8b9fc5442acdf89a6ea54de7d92b3a5941e8` (`main`, 2026-09-24) — the T5.1 Windows
acceptance record. No source, test or frozen file differs from the Phase-1 code-complete tip on which CI last
ran; the commit is documentation only, so the matrix below verifies the shipped code as well.

## 1. Result

| OS family | Role (plan) | `ctest` | Layer guard | Source of evidence |
| --- | --- | --- | --- | --- |
| **Windows** | primary target | **2/2 passed** (unit 7.01 s, smoke 0.06 s) | `OK (22 files scanned)` | CI `windows-latest` + local real-console run (`docs/t5.1-windows-acceptance.md`) |
| **Linux** | verified | **2/2 passed** (unit 1.87 s, smoke 0.00 s) | `OK (22 files scanned)` | CI `ubuntu-latest` |
| **macOS** | second opinion | **2/2 passed** (unit 2.31 s, smoke 0.03 s) | `OK (22 files scanned)` | CI `macos-latest` + the pty pass in `docs/W4_KIM_PROGRESS.md` |

The unit binary reports **`OK 117 tests`**; the portable `smoke` target runs the real `csopesy.exe` in
`--no-tty` plain-line mode and is the dev/CI half of A12, not a graded take.

**CI run — [36007416747](https://github.com/LorensTee/CSOPESY-MCO3/actions/runs/36007416747)** (push event,
head `dbfc8b9`), **completed / success** on all three jobs:

| Job | Job id | Steps | `ctest` |
| --- | --- | --- | --- |
| `build-and-test (ubuntu-latest)` | 107659077277 | Layer guard / Configure / Build / Test ✓ | `100% tests passed, 0 tests failed out of 2` |
| `build-and-test (windows-latest)` | 107659077476 | Layer guard / Configure / Build / Test ✓ | `100% tests passed out of 2` |
| `build-and-test (macos-latest)` | 107659077587 | Layer guard / Configure / Build / Test ✓ | `100% tests passed out of 2` |

The macOS job is the "second opinion": the threaded `unit` suite executes a real worker thread on
Apple Silicon, so the 2/2 is the threaded scheduler running on a third platform, not just compiling.

## 2. Windows (primary target)

* Local `--no-tty` plain-line ctest on the development build: `100% tests passed, 0 tests failed out of 2`
  (unit 2.09 s, smoke 0.07 s) — `ctest --preset debug`.
* The unit binary prints **`OK 117 tests`**.
* `bash scripts/check_layers.sh` → `check_layers: OK (22 files scanned)`.
* `git hash-object include/csopesy/*.hpp` → **11/11** match `CONTRACTS.md`; no frozen header touched.
* Warning-free build under GCC 14.2.0 (MinGW-W64 UCRT), CMake 3.30.4, Ninja 1.12.1.

Beyond `ctest`, the Windows A1–A11 acceptance was driven on a **real Win32 console** (not a pipe): see
`docs/t5.1-windows-acceptance.md`. That is the interactive half of the Windows verification and is stronger
than what the CI job alone provides.

## 3. Linux

The Linux half is **`ctest` via CI** — the plan names "the CI run URLs" as the evidence, and the
`ubuntu-latest` job runs the full configure / build / test path on the shipped commit. Result in §1.

**Honest scope.** The Linux column is a **CI build + `ctest`** verification. It does **not** include a human
A1–A11 pass on a Linux terminal, and none is claimed: the plan asks for `ctest` on each OS, and the Linux
`PosixTerminal` raw-mode/pty behaviour was separately pty-verified at T1.1. If a graded take on a Linux
terminal is later wanted, it belongs to whoever holds the Linux hardware; it is not blocked on anything in the
repository.

## 4. macOS (second opinion)

W4's record (`docs/W4_KIM_PROGRESS.md`, *Phase 2 — T5.2 macOS second-opinion verification*) drives the built
binary on a **real kernel pty** and validates every frame byte-exactly: 894 frames at 80×24, 71 for A8, 43 for
resize + 20×5, 18 for Ctrl+C, 1828 at `--refresh-ms=1 --poll-ms=1`, 22 at `--refresh-ms=10000`, **0 malformed,
exit 0, termios restored** in every scenario. The one acceptance/spec inconsistency it found (A4 non-ASCII)
is the same one recorded in the Windows T5.1 record and left unpatched.

## 5. Gates

| Gate | Result |
| --- | --- |
| Cross-OS `ctest` | **2/2 on Windows, Linux, macOS** |
| `check_layers.sh` | `OK (22 files scanned)` on all three |
| Frozen headers | **11/11** hashes match `CONTRACTS.md`; no header touched |
| Warning-free build | yes (local GCC 14.2.0; CI gcc/clang/MSVC) |
| CRLF / line-ending guard | commit warning only (docs text), no source effect |

## 6. What T5.2 does **not** cover

* The **frozen-binary** takes (`T3.3`, `T1.4` Linux sweep) and the **live CLion gate** (`T3.4`) — all need
  `frozen/csopesy.exe` (**T6.3**), which does not exist yet. Nothing is faked here.
* **T5.3** (tearing / typing-delay synthesis) — still scaffolding; needs the frozen binary's observations.
* Per-keystroke latency — never a T5.2 claim (D4: observed thresholds only; that wording binds T5.3).
