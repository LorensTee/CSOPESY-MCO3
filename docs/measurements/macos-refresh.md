# macos-refresh.md — the **refresh** sweep on macos

* **Recorder (owner):** W4 Kim — W4 macOS second opinion
* **Status:** **PENDING (T6.3)** — the sweep runs against the frozen binary only (D14). No values may be
  recorded before the artifact exists; see `README.md`.
* **Artifact under test:** `frozen/csopesy.exe` — SHA-256 **PENDING (T6.3)**

Copy the table shape from `TEMPLATE.md`, record one row per take, and change **only** the run
configuration's Program arguments between takes — never rebuild, never re-copy the executable.

## refresh table

PENDING (T6.3)

## Honest limitation

Observed thresholds and recommended values only — **not** per-keystroke latency figures (D4).
