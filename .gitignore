# --- Build output — generated, never source ---------------------------------------------------
build/
build-*/
Build/
frozen/

# --- CMake / CTest / CLion generated ----------------------------------------------------------
CMakeUserPresets.json
CMakeFiles/
CMakeCache.txt
cmake_install.cmake
install_manifest.txt
compile_commands.json
Testing/
.cache/

# --- Compiler, linker and debugger artifacts --------------------------------------------------
*.o
*.obj
*.a
*.lib
*.so
*.so.*
*.dylib
*.dll
*.exe
*.out
*.app
*.d
*.dwo
*.pdb
*.ilk
*.gch
*.pch
*.mod
*.smod

# --- CLion: the IDE state is local, the run configurations are COMMITTED (§T0.5) --------------
# .idea/runConfigurations/{csopesy-dev,csopesy-quiz}.xml ARE the graded run configuration and are
# part of the deliverable, so they must stay tracked.
.idea/*
!.idea/runConfigurations/

# --- Local scratch ----------------------------------------------------------------------------
*.tmp
*.log
*.bak
*.swp

# --- Deliberately NOT versioned: the design conversation in docs/replies/ ---------------------
# User-authored. Delete this line to version those notes; nothing in docs/ else is ignored.
/docs/replies/

# --- NOT versioned: the course handout PDF ----------------------------------------------------
# Third-party course material, and this repository is public — not ours to redistribute. §3.2's
# docs/ tree does not list it. Keep a local copy and share it with teammates directly.
/docs/MO3 - Marquee_Operator.pdf
